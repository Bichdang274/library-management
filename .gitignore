# --- 1. Dependencies (Thư viện - Không up code thư viện, nhưng GIỮ file lock) ---
node_modules/
# KHÔNG ĐƯỢC VIẾT package-lock.json vào đây!

# --- 2. Build Output (File biên dịch - Không cần thiết up) ---
dist/
build/
coverage/

# --- 3. Environment Variables (BẢO MẬT - TUYỆT ĐỐI KHÔNG UP) ---
.env
.env.local
.env.*.local
*.env

# --- 4. Backend Uploads (Giữ folder, chặn ảnh rác) ---
# Chặn tất cả file trong uploads
backend/uploads/*
# Nhưng giữ lại file .gitkeep để Git biết là cần giữ thư mục này
!backend/uploads/.gitkeep

# --- 5. Logs & System (File rác hệ thống) ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*